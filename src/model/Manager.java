package model;

public class Manager extends Person{
    public Manager(String name, String surname, int age, int id, PositionEnum position) {
        super(name, surname, age, id, position);
    }
}
